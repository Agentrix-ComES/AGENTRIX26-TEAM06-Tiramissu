# ayu_design

A new Flutter project.

## Getting Started

This project is a starting point for a Flutter application.

A few resources to get you started if this is your first Flutter project:

- [Lab: Write your first Flutter app](https://docs.flutter.dev/get-started/codelab)
- [Cookbook: Useful Flutter samples](https://docs.flutter.dev/cookbook)

For help getting started with Flutter development, view the
[online documentation](https://docs.flutter.dev/), which offers tutorials,
samples, guidance on mobile development, and a full API reference.

## Flutter Version Setup

This project is standardized on **Flutter 3.38.5**.

To maintain consistency and avoid conflicts with auto-generated files and dependency lockfiles:
* **FVM (Recommended):** Use FVM to run commands for this project. FVM will automatically use the version pinned in `.fvmrc`.
  ```bash
  fvm flutter pub get
  fvm flutter run
  ```
* **Manual SDK Management:** If you are not using FVM, ensure your active local Flutter SDK version is set to exactly `3.38.5`.
* **Avoid Auto-Fixes/Upgrades:** Please do not accept IDE prompts for automatic Flutter upgrades, package migrations, or general auto-fixes unless agreed upon by the team.

