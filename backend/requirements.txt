# ── Core framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── LangChain & LLM ────────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-google-genai>=1.0.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0

# ── STT providers (set the matching API key in .env to activate) ───────────────
groq>=0.9.0          # Groq Whisper — fastest option (GROQ_API_KEY)
openai>=1.30.0       # OpenAI Whisper fallback  (OPENAI_API_KEY)

# ── HTTP & networking ──────────────────────────────────────────────────────────
httpx>=0.27.0
python-multipart>=0.0.9   # required for FastAPI file uploads

# ── Data & vector search ───────────────────────────────────────────────────────
pydantic>=2.7.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
tiktoken>=0.7.0

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
supabase>=2.4.0
